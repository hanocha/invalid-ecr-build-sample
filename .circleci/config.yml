version: 2.1

orbs:
  aws-ecr: circleci/aws-ecr@6.12.2

jobs:
  build-image:
    docker:
      - image: circleci/golang:1.17.2
    steps:
      - checkout
      - aws-ecr/build-image:
          tag: "$CIRCLE_SHA1"
          repo: "invalid-ecr-build-sample"

workflows:
  main:
    jobs:
      - build-image:
          context: 
            - test
